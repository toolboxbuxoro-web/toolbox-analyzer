# 🚀 Инструкция по деплою приложения

## Вариант 1: Vercel (Рекомендуется - Бесплатно и просто)

### Шаги:

1. **Зарегистрируйтесь на [Vercel](https://vercel.com)**
   - Используйте GitHub аккаунт для быстрой регистрации

2. **Загрузите проект на GitHub** (если еще не загружен):
   ```bash
   git add .
   git commit -m "Готово к деплою"
   git push origin main
   ```
   
   Если у вас еще нет репозитория на GitHub:
   - Создайте новый репозиторий на [github.com](https://github.com/new)
   - Выполните команды:
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/toolbox-sklad.git
   git push -u origin main
   ```

3. **Импортируйте проект в Vercel:**
   - Откройте [vercel.com/new](https://vercel.com/new)
   - Нажмите "Import Git Repository"
   - Выберите ваш репозиторий `toolbox-sklad`
   - Нажмите "Deploy"
   
4. **Готово!** 🎉
   - Vercel автоматически соберет и задеплоит приложение
   - Вы получите ссылку вида: `https://toolbox-sklad.vercel.app`
   - Приложение автоматически обновится при каждом git push

---

## Вариант 2: Netlify (Альтернатива Vercel)

1. Зарегистрируйтесь на [Netlify](https://netlify.com)
2. Загрузите проект на GitHub (см. выше)
3. В Netlify нажмите "Add new site" → "Import from Git"
4. Выберите репозиторий
5. Настройки билда:
   - Build command: `npm run build`
   - Publish directory: `.next`
6. Нажмите "Deploy"

---

## Вариант 3: Локальный запуск (для использования только на вашем компьютере)

### Режим разработки:
```bash
npm run dev
```
Откройте: http://localhost:3000

### Production билд (оптимизированный):
```bash
npm run build
npm start
```
Откройте: http://localhost:3000

⚠️ **Внимание:** При закрытии терминала приложение остановится.

---

## Вариант 4: Production билд для локальной сети

Если хотите дать доступ другим компьютерам в вашей сети:

1. **Соберите приложение:**
   ```bash
   npm run build
   ```

2. **Запустите на всех интерфейсах:**
   ```bash
   npm start -- -H 0.0.0.0
   ```
   
   Или измените `package.json`:
   ```json
   "scripts": {
     "start": "next start -H 0.0.0.0"
   }
   ```

3. **Узнайте ваш локальный IP:**
   - macOS/Linux: `ifconfig | grep "inet "`
   - Windows: `ipconfig`

4. **Доступ:**
   - С вашего компьютера: http://localhost:3000
   - С других устройств в сети: http://ВАШ_IP:3000

---

## Вариант 5: Docker (для продвинутых)

1. **Создайте `Dockerfile`:**
   ```dockerfile
   FROM node:18-alpine
   WORKDIR /app
   COPY package*.json ./
   RUN npm install
   COPY . .
   RUN npm run build
   EXPOSE 3000
   CMD ["npm", "start"]
   ```

2. **Соберите и запустите:**
   ```bash
   docker build -t toolbox-sklad .
   docker run -p 3000:3000 toolbox-sklad
   ```

---

## 📝 Рекомендации

- **Для публичного доступа в интернете:** Используйте **Vercel** или **Netlify**
- **Для локального использования:** Запустите `npm run dev` или `npm run build && npm start`
- **Для компании/офиса:** Рассмотрите Docker + сервер или локальный запуск на выделенном компьютере

---

## 🔧 Устранение проблем

### Ошибка при билде:
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json .next
npm install
npm run build
```

### Приложение не открывается:
- Проверьте, что порт 3000 не занят
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте логи в терминале

---

## 🎉 Готово!

Выберите подходящий вариант и наслаждайтесь работой с приложением!

